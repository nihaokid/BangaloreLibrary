package com.thoughtworks.wzhai;

import org.junit.Test;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.assertThat;

public class UserTest {
    @Test
    public void testAuthenticated() throws Exception {
//        User user1 = new User("name", "email", "num");
//        assertThat(user1.getId(),is("111-1111"));
//        assertThat(user1.authenticated("1111"),is(true));
//        assertThat(user1.authenticated("1112"),is(false));
//        User user2 = new User("name", "email", "num");
//        assertThat(user2.getId(),is("111-1112"));
//        assertThat(user2.authenticated("1112"),is(true));
//        assertThat(user2.authenticated("1111"),is(false));
    }
}
